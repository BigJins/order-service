spring:
  application:
    name: order-service
  config:
    import: "optional:configserver:http://localhost:8888"

server:
  port: 8081

kafka:
  topics:
    delivery-completed: delivery.completed.v1